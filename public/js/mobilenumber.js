// const remote = require('electron').remote;
// const app = remote.app;
// const logger = remote.getGlobal('logger');
// logger.debug('[mobilenumber] loading from mobilenumber js')
// $('.bfh-selectbox-options li a').each(function(key, value) {
    //console.log($(this).text().replace(' (Spain)',''))
    // logger.debug('[mobilejs] dropbox value: %s', value);
    // logger.debug('[mobilejs] dropbox text: %s', $(this).text());
    // $(this).text($(this).text().replace('',''))
// });
